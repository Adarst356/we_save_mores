import 'package:flutter/material.dart';

class RefersScreen extends StatelessWidget {
  const RefersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("ReferScreen"),
      ),
    );
  }
}
